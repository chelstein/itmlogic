node_modules/
.env
.env.local
.env.*.local
*.log
.DS_Store
/tmp/
/coverage/                # test coverage reports (root-level only — src/engine/coverage/ is engine code)

# Vite build output (generated; do not commit)
src/ui/dist/
