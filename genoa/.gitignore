node_modules/
.env
.env.local
.env.*.local
*.log
.DS_Store
/tmp/
/coverage/                # test coverage reports (root-level only — src/engine/coverage/ is engine code)

# Vite build output — committed so deployment doesn't require a rebuild step
# src/ui/dist/

# Graphify generated artifacts
graphify-out/

# Local virtualenv
.venv/

# Local Claude instructions
CLAUDE.md
